# ================================
# CI / 安全测试所需依赖（requirements.txt 的精简子集）
# 用于 GitHub Actions 中运行 tests/security
# ================================

# Web 框架
fastapi>=0.111.0
uvicorn>=0.29.0
pydantic>=2.7.0

# 日志
loguru>=0.7.0

# 网络通信
websockets>=10.0,<13.0
aiohttp>=3.9.0
requests>=2.31.0
httpx>=0.25.0

# 配置 / 图像 / 二维码
PyYAML>=6.0.0
Pillow>=10.0.0
qrcode[pil]>=7.4.2

# 加密与认证
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
cryptography>=41.0.0

# 数据处理
pandas>=2.0.0
openpyxl>=3.1.0

# 文件上传 / 邮件校验
python-multipart>=0.0.6
email-validator>=2.0.0

# AI 回复引擎
openai>=1.50.0

# 系统监控
psutil>=5.9.0

# JS 运行时 / 协议解析（reply_server 导入链需要）
blackboxprotobuf>=1.0.1
PyExecJS>=1.5.1

# 浏览器自动化（仅需 Python 包，CI 不下载浏览器）
playwright>=1.40.0

# 测试框架
pytest>=8.0.0
